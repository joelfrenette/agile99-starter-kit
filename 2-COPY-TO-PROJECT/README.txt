STEP 2 — COPY THESE FILES TO YOUR PROJECT
==========================================
Pick the rules file for YOUR tool. Always copy CONTEXT.md and PROMPT-LOG.md.

ALL TOOLS — always copy these 2 files:
  CONTEXT.md           <- living project brain (all tools read this)
  PROMPT-LOG.md        <- self-improving prompt log (all tools read this)

THEN copy the rules file for your tool:

V0.APP:
  AI_RULES.md          -> project root, push to GitHub

DYAD.SH:
  AI_RULES.md          -> project root folder (overwrites default)

WINDSURF:
  .windsurfrules        -> project root folder

VS CODE + COPILOT:
  .github/
    copilot-instructions.md  -> copy the whole .github/ folder

CURSOR:
  .cursor/
    rules/agile99.mdc   -> copy the whole .cursor/ folder
  .cursorrules          -> also include (legacy fallback)

CLAUDE CODE:
  CLAUDE.md            -> project root

CHATGPT CODEX:
  AGENTS.md            -> project root (also works with Antigravity + Cursor)

GOOGLE ANTIGRAVITY:
  GEMINI.md            -> project root (highest priority)
  AGENTS.md            -> project root (cross-platform, also keep this)


OPENCLAW:
  See 4-REFERENCE/PROMPTS/openclaw-setup.md for agent SKILL.md setup

See 1-START-HERE/START-HERE.html for full step-by-step setup for each tool.
